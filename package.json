{
  "name": "cross-platform-nodejs-test",
  "version": "1.0.0",
  "description": "Demonstration Node.js project for cross-platform GitHub Actions matrix testing",
  "main": "src/index.js",
  "scripts": {
    "test": "mocha --reporter mocha-junit-reporter --reporter-options mochaFile=./test-results/results.xml"
  },
  "dependencies": {},
  "devDependencies": {
    "mocha": "^10.0.0",
    "mocha-junit-reporter": "^2.2.0"
  }
}
